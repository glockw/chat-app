import { RowLengthDirective } from './row-length.directive';

describe('RowLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new RowLengthDirective();
    expect(directive).toBeTruthy();
  });
});
